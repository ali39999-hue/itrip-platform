# iTrip / Firuzo Travel Platform — سند جامع ممیزی فنی و نقشه راه لانچ محصول (Production Roadmap)

> **نسخه سند:** 2.0.0  
> **تاریخ بازبینی و تدوین:** سپتامبر ۲۰۲۶  
> **مخاطبان:** تیم مهندسی نرم‌افزار، مدیران محصول، معماران سیستم، تیم DevOps و ذینفعان کسب‌وکار  
> **وضعیت کدپایه جاری:** Next.js 16.3 (App Router) | React 19.2 | TypeScript 5+ | PostgreSQL 16 | Prisma ORM (۶۲ مدل، ۲۱ مایگریشن) | ۴۹ سوئیت تستی (۳۲۱ تست واحد و یکپارچگی سبز)

---

## ۱. خلاصه اجرایی (Executive Summary)

پروژه **iTrip / Firuzo** یک پلتفرم پیشرفته و جامع رزرواسیون آنلاین سفر (شامل پرواز، هتل، تورهای گردشگری و پنل سازمانی ERP) است که بر پایه معماری **Modular Monolith** و رویکرد **Domain-Driven Design (DDD)** بنا شده است. 

سیستم در لایه‌های هسته مالی (Financial Kernel) با استفاده از حسابداری دوبل (Double-Entry General Ledger)، الگوی تراکنش مالی مبتنی بر `Prisma.Decimal`، مدیریت همزمانی انبار (Pessimistic Row-Locking)، ثبت لاگ وضعیت‌های رابطه‌ای (`BookingStatusHistory`) و الگوی جعبه ارسال پیام (`Transactional Outbox`) ساختاری با استاندارد بسیار بالا دارد.

با این حال، برای تبدیل این هسته مهندسی قوی به یک **محصول نهایی قابل بهره‌برداری در مقیاس تجاری (Production-Grade Live Product)**، ۳۰ چالش ساختاری در ۶ دسته اصلی شناسایی شده‌اند که باید طی یک برنامه زمان‌بندی ۴ تا ۵ ماهه پیاده‌سازی و نهایی شوند.

---

## ۲. دسته‌بندی و تحلیل ۳۰ مشکل شناسایی‌شده با راهکارهای عملی و کدهای نمونه

---

### دسته اول: مشکلات بحرانی و مسدودکننده (P0 - Critical Blocker)

#### ۱. عدم یکپارچگی واقعی با درگاه‌های بانکی و PSPها
* **شرح مشکل:** آداپتورهای درگاه پرداخت (`ShetabGatewayAdapter`) در وضعیت شبیه‌سازی قرار دارند و توکن‌های پرداخت را به صورت موک تولید می‌کنند. هیچ تراکنش واقعی با شبکه شاپرک (سامان، به‌پرداخت، سداد، زرین‌پال) یا شبکه بین‌المللی (Stripe) انجام نمی‌شود.
* **ریسک:** عدم امکان دریافت وجه واقعی و مسدود شدن کامل مدل درآمدی پلتفرم.
* **راهکار:** انعقاد قرارداد رسمی با حداقل دو PSP (برای افزونگی)، پیاده‌سازی آداپتورهای HTTP با احراز هویت دوجانبه (mTLS/API Key)، مکانیزم اعتبارسنجی وب‌هوک با امضای HMAC-SHA256 و تطابق پنجره زمانی (Replay Protection).
* **کد نمونه پیاده‌سازی آداپتور واقعی:**
```typescript
// src/domains/payment/adapters/sep-gateway-adapter.ts
import { GatewayPort, PaymentRequest, PaymentRedirectResult, PaymentVerificationResult } from '../gateway-port';
import crypto from 'crypto';

export class SepGatewayAdapter implements GatewayPort {
  private readonly terminalId = process.env.SEP_TERMINAL_ID!;
  private readonly secretKey = process.env.SEP_SECRET_KEY!;
  private readonly baseUrl = process.env.SEP_BASE_URL || 'https://sep.shaparak.ir';

  async initiatePayment(request: PaymentRequest): Promise<PaymentRedirectResult> {
    const payload = {
      action: 'token',
      terminalId: this.terminalId,
      amount: request.amount.toNumber(),
      resNum: request.paymentIntentId,
      redirectUrl: request.callbackUrl,
      cellNumber: request.customerPhone,
    };

    const res = await fetch(`${this.baseUrl}/onlinePG/onlinePG`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
    });

    if (!res.ok) throw new Error(`PSP Connection Error: ${res.statusText}`);
    const data = await res.json();
    if (data.status !== 1) throw new Error(`PSP Token Error: ${data.errorCode}`);

    return {
      redirectUrl: `${this.baseUrl}/OnlinePayment.aspx?token=${data.token}`,
      paymentIntentId: request.paymentIntentId,
      token: data.token,
    };
  }

  async verifyPayment(token: string, refNum: string): Promise<PaymentVerificationResult> {
    const res = await fetch(`${this.baseUrl}/verifyTxnRandomSessionkey/ipg/VerifyTransaction`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ RefNum: refNum, TerminalNumber: this.terminalId }),
    });
    const result = await res.json();
    return {
      verified: result.ResultCode === 0,
      status: result.ResultCode === 0 ? 'CAPTURED' : 'FAILED',
      trackingCode: result.TransactionDetail?.RRN || refNum,
    };
  }
}
```

#### ۲. عدم اتصال زنده به سیستم‌های توزیع جهانی (GDS) و بانک‌های هتل (BedBanks)
* **شرح مشکل:** داده‌های جستجوی پرواز و هتل از کاتالوگ لوکال دیتابیس واکشی می‌شوند و استعلام زنده از Amadeus, Sabre, Turkish Airlines NDC, Hotelbeds یا وب‌سرویس‌های داخلی (پارتو، گابریل، مقصود) وجود ندارد.
* **ریسک:** عدم امکان صدور واقعی بلیط یا ووچر هتل و مغایرت ظرفیت با موجودی واقعی تأمین‌کننده.
* **راهکار:** پیاده‌سازی خطوط لوله نرمال‌سازی داده برای پروازها و هتل‌ها، استفاده از الگوی Circuit Breaker و Caching چندلایه‌ای با Redis جهت جلوگیری از هزینه بالای API تأمین‌کنندگان.

#### ۳. جداسازی ورکرها از پروسس وب (Worker Process Decoupling)
* **شرح مشکل:** پردازش رویدادهای صف ارسال (`OutboxConsumer`)، انقضای نگه‌داشت‌های منقضی‌شده (`HoldExpirationWorker`) و بازیابی ساگاها در حال حاضر درون پروسس وب (Next.js server) با فایل `instrumentation.ts` اجرا می‌شوند.
* **ریسک:** در محیط‌های Multi-Instance یا سرورلس (Serverless)، ورکرها یا تکراری اجرا شده و منابع را قفل می‌کنند، یا اصلاً اجرا نمی‌شوند و Single Point of Failure ایجاد می‌کنند.
* **راهکار:** تفکیک کانتینر ورکر در داکر و کوبرنتیز با اجرای دستور مستقل `npm run worker` بر پایه `src/workers/worker-entrypoint.ts` و استفاده از توزیع کار با لیس توزیع‌شده (`WorkerLeaseService` و `FOR UPDATE SKIP LOCKED`).

#### ۴. مانیفست‌های کامل استقرار ابری (Kubernetes & Production Container Manifests)
* **شرح مشکل:** اگرچه `Dockerfile` و `docker-compose.yml` در پروژه ایجاد شده‌اند، اما مانیفست‌های استقرار روی کلاسترهای کوبرنتیز (K8s Deployments, StatefulSets, HPA, Ingress, Secrets) در رپازیتوری موجود نیستند.
* **ریسک:** ایجاد چالش‌های بزرگ و Downtime در فرآیند استقرار در مقیاس Enterprise و ناتوانی در Auto-scaling خودکار در زمان پیک ترافیک.
* **راهکار:** ایجاد پوشه استانداردهای `k8s/` شامل تعاریف Deployment پروسس وب، Deployment ورکرها، اینگرس با SSL، تنظیمات Secret و قوانین HPA (Horizontal Pod Autoscaler).

---

### دسته دوم: مشکلات مهم ساختاری و داده‌ای (P1 - Major Architectural Issues)

#### ۵. مقیاس‌پذیری و توزیع‌شدگی Rate Limiting
* **شرح:** سیستم ریت‌لیمیت دارای Fallback حافظه‌ای است اما در کلاسترهای ابری چند نودی، شمارنده‌ها باید منحصراً در کلاستر Redis همگام شوند تا از حملات توزیع‌شده کلاهبرداری OTP و اسپم بوکینگ جلوگیری شود.

#### ۶. تکمیل چرخه بین‌المللی‌سازی (i18n) و رفع فراخوانی‌های سنتی
* **شرح:** ساختار `next-intl` با ۵ زبان (fa, en, ar, zh, ru) فعال است، اما برخی متون در بخش ادمین و کامپوننت‌های فرعی همچنان از تابع لگسی `lt()` استفاده می‌کنند که باید کاملاً با کلیدهای یکپارچه `messages/*.json` جایگزین شوند.

#### ۷. اتصال به وب‌سرویس لحظه‌ای نرخ برابری ارز (Live FX Feeds)
* **شرح:** نرخ‌های تسعیر ارز در حال حاضر با مقادیر ثابت (`StaticRateProvider`) کار می‌کنند. سیستم باید به APIهای رسمی بانک مرکزی یا OpenExchangeRates متصل شده و اسنپ‌شات لحظه‌ای نرخ ارز را به عنوان سند مالی در رکورد بوکینگ ثبت کند.

#### ۸. دیده‌پذیری کامل (Full Observability & APM)
* **شرح:** لاگر جیسون و معیارهای سلامت پایه وجود دارند، اما برای پروداکشن نیاز به اضافه شدن ترکینگ توزیع‌شده (OpenTelemetry Tracing)، یکپارچه‌سازی با Sentry جهت کشف خطاهای کلاینت و سرور، و داشبورد متریک‌های بلادرنگ بیزنس (نرخ موفقیت درگاه، زمان پاسخگویی تأمین‌کنندگان) است.

#### ۹. تست‌های احراز هویت‌شده سرتاسری (E2E Test Coverage)
* **شرح:** تست‌های Playwright برای مسیرهای بدون لاگین وجود دارند، اما جریان‌های حساس خرید ادمین، مدیریت مغایرت‌های مالی و کنسلی چندمرحله‌ای نیازمند تست‌های E2E خودکار با کاربرهای اعتبارسنجی‌شده هستند.

#### ۱۰. مستندسازی استراتژی پشتیبان‌گیری و بازیابی اضطراری (Disaster Recovery & PITR)
* **شرح:** ایجاد Runbook عملیاتی برای بازگردانی لحظه‌ای دیتابیس (Point-In-Time Recovery) و تست دوره‌ای اسکریپت‌های بازیابی پشتیبان از روی آبجکت استوریج‌های امن S3/MinIO.

#### ۱۱. جداسازی سرسختانه داده‌های چندمستأجری (B2B Multi-Tenant Isolation)
* **شرح:** تضمین اعمال فیلتر `organizationId` در تمام کوئری‌های پریزما با استفاده از Prisma Client Extensions تا از هرگونه نشت تصادفی اطلاعات آژانس‌ها جلوگیری شود.

---

### دسته سوم: بهبودهای تخصصی کاربری و رابط بصری (UI/UX Excellence)

#### ۱۲. انطباق کامل با استانداردهای دسترسی‌پذیری (A11Y - WCAG 2.1 AA)
* **اقدام:** تکمیل ویژگی‌های `aria-expanded`، `role="combobox"` و مدیریت فوکوس صفحه‌کلید (Tab & Arrow navigation) در ویجت‌های جستجو و تقویم دوزبانه هجری شمسی و میلادی.

#### ۱۳. بهینه‌سازی بارگذاری و عملکرد فرانت‌اند (Web Vitals Optimization)
* **اقدام:** اعمال Code Splitting پویا روی کامپوننت‌های سنگین (نقشه‌های Leaflet، ابزارهای رندر PDF و نمودارهای مالی ERP)، زیرمجموعه‌سازی فونت‌ها (Font Subsetting) برای ایران‌سنس/ایران‌یکان و بهره‌گیری از اولویت‌بندی تصویر در Next.js Image.

#### ۱۴. پیشرفته‌سازی موتور فیلتر و جستجوی پروازها و هتل‌ها
* **اقدام:** افزودن اسلایدر بازه زمانی پرواز (Departure/Arrival time window)، تفکیک خطوط هوایی، فیلتر ستاره هتل و بار مجاز همراه، همراه با لودینگ‌های Skeleton فوق‌العاده روان.

#### ۱۵. ساده‌سازی و شفاف‌سازی فرآیند Checkout
* **اقدام:** بازطراحی فرم ورود مشخصات مسافران، امکان ذخیره و پرکردن خودکار پروفایل مسافران همسفر، نمایش شمارنده معکوس قفل قیمت (Soft-Lock Timer: 15:00) و شفاف‌سازی کامل قوانین جریمه کنسلی قبل از پرداخت نهایی.

#### ۱۶. بازطراحی صفحات جزئیات محصول (Product Detail Pages)
* **اقدام:** نمایش خط سیر توقف‌های پرواز روی نمودار بصری زیبا، مشخص کردن کلاس‌های نرخی (Economy Value, Flexi)، امکان انتخاب امکانات اتاق هتل (صبحانه، تخت اضافه) با محاسبه لحظه‌ای قیمت.

#### ۱۳. بازطراحی تجربه کاربری موبایل (Mobile-First Journey)
* **اقدام:** پیاده‌سازی Bottom Sheets مدرن به جای مودال‌های پاپ‌آپ، ناوبری شناور انگشت شست (Thumb-zone optimized navigation) و عملکرد عالی در اینترنت‌های با پهنای باند ضعیف.

#### ۱۸. وضعیت‌های خالی هدفمند و راهنمایی کاربران (Empty States & Onboarding)
* **اقدام:** پیام‌های کمکی شفاف هنگام نیافتن نتیجه جستجو، پیشنهاد مقاصد پرطرفدار نزدیک، و تور آموزشی سریع برای کاربران جدید پنل B2B.

---

### دسته چهارم: زیرساخت و DevOps

#### ۱۹. خطوط لوله CI/CD بدون نقص (Automated Release Gates)
* **اقدام:** تست خودکار در گیت‌هاب اکشنز شامل: بررسی لایسنس‌ها، اسکن سکرت‌ها با Gitleaks، تست مایگریشن روی کانتینر واقعی دیتابیس، Typecheck دقیق، اجرای تست‌های واحد و اجرای تست‌های E2E.

#### ۲۰. مدیریت متمرکز سکرت‌ها و کلیدهای رمزنگاری
* **اقدام:** انتقال کلیدهای خصوصی، توکن‌های ربات‌ها، سکرت‌های اعتبارسنجی Auth و کلیدهای وب‌سرویس‌ها به سیستم‌های امن مدیریت کلید (Vault / AWS KMS / Doppler).

#### ۲۱. محیط‌های ایزوله استیجینگ و پیش‌نمایش (Preview Deployments)
* **اقدام:** ایجاد محیط Staging دقیقاً مشابه Production با اتصال به ساندباکس بانک‌ها جهت تست رگرسیون قبل از هر ریلیز پروداکشن.

---

### دسته پنجم: امنیت، انطباق و اعتبارسنجی (Security & Compliance)

#### ۲۲. رمزنگاری فیلدهای حساس و داده‌های هویتی (PII Protection)
* **اقدام:** ذخیره شماره گذرنامه، کد ملی، و تاریخ انقضا در دیتابیس با رمزنگاری قرینه معتبر (AES-256-GCM) از طریق `CryptoVault` و لاگ دسترسی کاربران به این اطلاعات (`TravelDocumentAudit`).

#### ۲۳. ایمن‌سازی سرور و هدرهای امنیتی (Defensive Hardening)
* **اقدام:** اعمال سخت‌گیرانه Content Security Policy (CSP)، HSTS، محدودسازی کامل CORS، و جلوگیری از حملات CSRF و Clickjacking روی تمامی روت‌های وب و APIها.

#### ۲۴. ضوابط سطح دیتابیس برای دفترکل (Ledger Integrity Invariants)
* **اقدام:** ایجاد تریگرها و Check Constraintهای صریح در PostgreSQL برای تضمین همیشگی برابری بدهکار و بستانکار (`Sum(Debit) = Sum(Credit)`) در هر سند حسابداری (`JournalEntry`).

#### ۲۵. ردپای حسابرسی تغییرات (Audit Trail & Event Log)
* **اقدام:** ثبت تغییرات وضعیت‌های حساس سیستم (تغییر موجودی کیف پول، کنسلی رزرو، بازپرداخت وجه، تغییر نقش کاربران) در جدول تغییرناپذیر لاگ‌های امنیتی همراه با شناسه کاربر، IP و زمان سرور.

---

### دسته ششم: عملکرد و مقیاس‌پذیری (Performance & Scalability)

#### ۲۶. ایندکس‌گذاری بهینه و پولینگ اتصالات (Connection Pooling)
* **اقدام:** پیاده‌سازی PgBouncer در لایه پایگاه داده و تعریف ایندکس‌های ترکیبی (Composite Indexes) روی جداول پرترافیک مثل `Booking(tenantId, status, createdAt)` و `InventoryHold(expiresAt, status)`.

#### ۲۷. کشینگ چندلایه داده‌های عمومی (Multi-Tier Caching)
* **اقدام:** کش کردن اطلاعات هتل‌ها، ایرلاین‌ها، و لیست فرودگاه‌ها در لایه کلاینت با Stale-While-Revalidate و در لایه سرور در حافظه Redis با TTLهای هوشمند.

#### ۲۸. تضمین صفر بودن رزرو مازاد (Zero Oversell Guarantee)
* **اقدام:** اعمال قفل سطر دیتابیس (`SELECT ... FOR UPDATE`) در ترکیب با بروزرسانی‌های اتمیک شرطی (`UPDATE allotment SET booked = booked + N WHERE booked + N <= capacity`).

---

### دسته هفتم: جریان داده و یکپارچگی تراکنشی (Data & Event Consistency)

#### ۲۹. صف ارسال تراکنشی پایدار (Transactional Outbox Pattern)
* **اقدام:** درج رویدادهای سیستمی در همان تراکنش ذخیره بوکینگ در جدول `OutboxEvent` و تخلیه ترتیبی آن با مکانیزم اختصاصی `FOR UPDATE SKIP LOCKED` و صف پیام‌های ناموفق (Dead-Letter Queue).

#### ۳۰. هماهنگ‌کننده ساگا و بازیابی در صورت شکست (Distributed Saga Orchestrator)
* **اقدام:** تفکیک تراکنش‌های بانکی و وب‌سرویس‌های خارجی از تراکنش‌های دیتابیس داخلی و اجرای تراکنش‌های جبران‌ساز (Compensating Transactions) در صورت بروز هرگونه شکست در هر مرحله از رزرو.

---

## ۳. برنامه زمان‌بندی ۴ تا ۵ ماهه پیاده‌سازی (Roadmap Phases)

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                   iTrip Production Launch Roadmap (20 Weeks)                            │
├───────────────────┬───────────────────┬───────────────────┬───────────────────┬─────────────────────────┤
│    ماه اول        │     ماه دوم       │     ماه سوم       │     ماه چهارم     │        ماه پنجم         │
│   (هفته ۱ تا ۴)   │   (هفته ۵ تا ۸)   │  (هفته ۹ تا ۱۲)   │  (هفته ۱۳ تا ۱۶)  │     (هفته ۱۷ تا ۲۰)     │
├───────────────────┼───────────────────┼───────────────────┼───────────────────┼─────────────────────────┤
│ فاز ۱: زیرساخت،  │ فاز ۲: اتصال     │ فاز ۳: ارتقای     │ فاز ۴: پایش،      │ فاز ۵: تست‌های نهایی،   │
│ امنیت و داکر/K8s  │ وب‌سرویس تأمین   │ تجربه کاربری      │ عملکرد و اسکیل    │ ممیزی و Rollout         │
│                   │ و درگاه بانکی     │ (UI/UX و چک‌اوت)  │ (APM & Caching)   │ پروداکشن                │
└───────────────────┴───────────────────┴───────────────────┴───────────────────┴─────────────────────────┘
```

### فاز ۱: زیرساخت، ایزوله‌سازی ورکرها و استقرار ابری (هفته ۱ تا ۴)
* تکمیل مانیفست‌های کوبرنتیز (`k8s/`) و تست داکر کانتینرها.
* جداسازی کامل پروسس ورکرها (`npm run worker`) در یک کانتینر مستقل.
* استقرار کلاستر Redis برای ریت‌لیمیتر توزیع‌شده و کشینگ.
* راه‌اندازی پایپ‌لاین CI/CD و اسکن خودکار امنیتی.

### فاز ۲: یکپارچه‌سازی درگاه‌های پرداخت و وب‌سرویس‌های سفر (هفته ۵ تا ۸)
* اتصال به وب‌سرویس حداقل یک درگاه بانکی معتبر ایرانی (سپ / به‌پرداخت / زرین‌پال) و درگاه ارزی.
* اتصال آداپتور تأمین‌کننده زنده برای پروازها و هتل‌ها به وب‌سرویس ساندباکس تأمین‌کنندگان سفر.
* پیاده‌سازی وب‌هوک‌های امن با اعتبارسنجی قطعی ضد دستکاری مبلغ.
* فعال‌سازی فید زنده نرخ برابری ارز (Live FX).

### فاز ۳: ارتقای سطح کیفی فرانت‌اند و تجربه کاربری (هفته ۹ تا ۱۲)
* اعمال استانداردهای دسترس‌پذیری A11Y در کل رابط کاربری.
* بازطراحی جریان رزرو و نهایی‌سازی خرید (Checkout Flow).
* بهینه‌سازی بارگذاری المان‌ها، فونت‌ها و تصاویر (Core Web Vitals).
* تطبیق کامل ترجمه‌های ۵ زبانه در تمام بخش‌های ادمین و کلاینت.

### فاز ۴: پایش، بهینه‌سازی دیتابیس و مدیریت خطا (هفته ۱۳ تا ۱۶)
* اتصال Sentry و راه‌اندازی داشبورد متریک‌های بلادرنگ بیزنس.
* بهینه‌سازی ایندکس‌های PostgreSQL و راه‌اندازی PgBouncer.
* تست‌های بارگذاری (Stress Testing) روی انبارداری پرواز و هتل با ۱۰۰۰ کاربر همزمان.
* تکمیل سیستم مدیریت مغایرت‌های مالی در پنل مدیریت ERP.

### فاز ۵: آزمون‌های امنیتی، رگرسیون نهایی و لانچ محصول (هفته ۱۷ تا ۲۰)
* اجرای آزمون نفوذپذیری مستقل (Penetration Test).
* اجرای کامل تست‌های سرتاسری Playwright روی کلیه سناریوهای کاربری.
* مانور تست بازیابی اضطراری دیتابیس (Disaster Recovery Simulation).
* راه‌اندازی آزمایشی (Soft Launch) با کاربران محدود و سپس رونمایی عمومی (Public Launch).

---

## ۴. ساختار تیم فنی و بودجه‌بندی تخصصی

| نقش تخصصی | تعداد نفرات | سهم تمرکز و بودجه | مسئولیت‌های کلیدی |
|---|:---:|:---:|---|
| **مهندس ارشد بک‌اند (Backend Lead)** | ۲ نفر | **۴۰٪** | معماری ساگا، یکپارچه‌سازی درگاه و GDS، دفترکل و انبارداری اتمیک |
| **مهندس ارشد فرانت‌اند (Frontend Lead)** | ۲ نفر | **۲۵٪** | Next.js 16، بازطراحی Checkout، بهینه‌سازی Web Vitals و A11Y |
| **مهندس زیرساخت و دواپس (DevOps / SRE)** | ۱ نفر | **۱۵٪** | کلاستر کوبرنتیز، مانیتورینگ APM، بکاپ‌های PITR و خط لوله CI/CD |
| **مهندس تضمین کیفیت و تست (QA / SDET)** | ۱ نفر | **۱۵٪** | تست‌های E2E با Playwright، تست بارگذاری با k6، ممیزی سناریوها |
| **متخصص امنیت نرم‌افزار (SecOps)** | پاره‌وقت | **۵٪** | ممیزی نفوذپذیری، ایمن‌سازی کلیدها، بازبینی رمزنگاری PII |

---

## ۵. چک‌لیست نهایی ۴۰+ آیتمی قبل از لانچ پروداکشن (Pre-Launch Verification Gate)

### ۱. کارکردی و هسته رزرو (Functional & Core Booking)
- [ ] تست کامل چرخه خرید پرواز از جستجو تا صدور بلیط.
- [ ] تست کامل چرخه خرید هتل از جستجو تا صدور واچر قطعی.
- [ ] اعتبارسنجی صفر بودن رزرو مازاد (Zero Oversell) تحت تست استرس همزمان.
- [ ] انقضای خودکار نگه‌داشت‌های موقت (Hold Sweeper) پس از اتمام زمان مجاز.
- [ ] فعال بودن سیستم قیمت‌گذاری پویا و اسنپ‌شات قیمت در فاکتور.
- [ ] صحت محاسبه کارمزد آژانس‌ها و تخفیف‌های ویژه سازمانی در لایه B2B.

### ۲. مالی و پرداخت (Payments & Financial Integrity)
- [ ] اعتبارسنجی ضد دستکاری مبلغ در وب‌هوک درگاه بانکی.
- [ ] جلوگیری از حملات بازپخش (Replay Attacks) در تراکنش‌های مالی.
- [ ] برابری دقیق بدهکار و بستانکار در تمامی اسناد دفترکل حسابداری.
- [ ] استرداد خودکار وجه (Refund Workflow) با کسر قانونی جریمه کنسلی.
- [ ] شارژ آنی و معتبر کیف پول اعتباری با سقف اعتباری مجاز.
- [ ] قفل سطری رکوردهای مالی در تراکنش‌های همزمان جهت پیشگیری از منفی شدن حساب.

### ۳. امنیت و انطباق (Security & Compliance)
- [ ] فعال بودن هشینگ رمز عبور با الگوریتم امن bcryptjs.
- [ ] رمزنگاری تمامی فیلدهای هویتی (شماره گذرنامه، کدملی) با AES-256-GCM.
- [ ] محدودیت نرخ درخواست برای ارسال کد اعتبارسنجی OTP و ورود.
- [ ] عدم نشت اطلاعات بین مستأجران مختلف در پنل سازمانی B2B (Tenant IDOR Isolation).
- [ ] اعمال هدرهای امنیتی سخت‌گیرانه (CSP, HSTS, X-Frame-Options, CORS).
- [ ] ممیزی و اسکن عدم وجود کلیدهای محرمانه و توکن‌ها در رپازیتوری کد.

### ۴. کارایی و مقیاس‌پذیری (Performance & Scalability)
- [ ] زمان پاسخ‌دهی سرور (TTFB) زیر ۲۰۰ میلی‌ثانیه برای صفحات اصلی.
- [ ] نمره Core Web Vitals بالای ۹۰ در لایت‌هاوس برای فرانت‌اند.
- [ ] زیرمجموعه‌سازی فونت‌ها و بهینه‌سازی فونت‌های فارسی و انگلیسی.
- [ ] مدیریت اتصالات دیتابیس با لایه کانکشن پولینگ (Connection Pooling).
- [ ] فعال بودن کش لایه‌ای برای اطلاعات پایه فرودگاه‌ها، شهرها و هتل‌ها.
- [ ] مقاومت سرویس تحت بار ۱۰۰۰ کاربر همزمان بر ثانیه.

### ۵. پایداری و دیده‌پذیری (Reliability & Observability)
- [ ] ارسال لاگ‌های ساختاریافته به سیستم لاگینگ متمرکز (Loki / Elasticsearch).
- [ ] ثبت و اطلاع‌رسانی بلادرنگ خطاهای ناشناخته در پنل Sentry.
- [ ] تنظیم هشدارهای خودکار (Alerts) برای زمان‌های پاسخ‌دهی بالای درگاه یا خطای ۵۰۰.
- [ ] اندپوینت‌های سلامت `/api/health/live` و `/api/health/ready` متصل به پروب‌های کوبرنتیز.
- [ ] اجرای مستقل ورکرها با قابلیت بازیابی خودکار پس از کرش.

### ۶. تجربه کاربری و دسترسی‌پذیری (UX & Accessibility)
- [ ] عدم وجود هرگونه متن ترجمه‌نشده در ۵ زبان اصلی پلتفرم.
- [ ] چیدمان بی‌نقص راست‌به‌چپ (RTL) و چپ‌به‌راست (LTR).
- [ ] عملکرد روان فرم‌ها و دکمه‌ها روی گوشی‌های هوشمند در رزولوشن‌های مختلف.
- [ ] ناوبری کامل با کیبورد برای افراد دارای محدودیت‌های جسمی (A11Y compliance).
- [ ] نمایش پیام‌های راهنمای صریح هنگام خطاهای شبکه یا عدم اتصال اینترنت.

### ۷. عملیات، پشتیبان‌گیری و نگهداری (Operations & Runbooks)
- [ ] تهیه خودکار نسخه‌های پشتیبان ساعتی و روزانه از دیتابیس روی فضای ذخیره‌سازی ابری امن.
- [ ] اجرای موفق حداقل یک سناریوی بازگردانی آزمایشی پشتیبان در محیط تست (Restore Drill).
- [ ] مستندسازی مستمر مراحل استقرار، دیباگ و کانفیگ سیستم در فایل‌های فنی.
- [ ] تفکیک شفاف متغیرهای محیطی پروداکشن، استیجینگ و توسعه در بسترهای امن.
